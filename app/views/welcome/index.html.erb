<div class="wrapper wrapper_padding clearfix">
	<div class="col-1 homepage_content">
		<h2>LET'S LEARN RUBY ON RAILS TOGETHER</h2>
		<hr>

		<% @posts.each do |post|%>
			<h3><a href="#"><%= link_to post.title, post %></a></h3>
			<p class="date"><%= post.created_at.strftime("%A, %b, %d") %> | <%= link_to post.user.fullname, profile_path(post.user.user_name) %></p>

		<% end %>
	</div>
	<div class="col-3 homepage_content">

	</div>	
</div><!--.wrapper-->